name: Validate container smoke test
description: Build the production image and verify startup, restart, persistence, and migrations.

runs:
  using: composite
  steps:
    - name: Run container smoke test
      shell: bash
      run: bash scripts/container-smoke.sh
