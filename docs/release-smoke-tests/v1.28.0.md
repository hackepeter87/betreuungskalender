# v1.28.0 published artifact smoke test

Date: 2026-09-03

Release: <https://github.com/hackepeter87/betreuungskalender/releases/tag/v1.28.0>

## Scope

This smoke test validated the published `v1.28.0` artifacts outside the normal
release workflow. It used temporary directories, isolated container volumes,
and fictional records only. No installation or cluster was changed.

## Reviewed commit and workflows

- Release commit: `2b0a7f2f086998e6792cee6f0e64d4f4298a3a99`
- Release archive validation:
  <https://github.com/hackepeter87/betreuungskalender/actions/runs/33728521371>
- Image publication:
  <https://github.com/hackepeter87/betreuungskalender/actions/runs/33728751224>
- Chart publication:
  <https://github.com/hackepeter87/betreuungskalender/actions/runs/33728751328>
- Testing-channel promotion:
  <https://github.com/hackepeter87/betreuungskalender/actions/runs/33729015996>

All workflows completed successfully from the reviewed release commit.

## Archive verification

The release archive and checksum were downloaded from the release validation
workflow. SHA-256 verification passed:

```text
9a805b40efaebe8578b0338f9c1b3fd279af118e439d3d74ab5e5e713c7b878d  betreuungskalender-v1.28.0.tar.gz
```

The archive contained the expected application build, release container file,
Compose definitions, Helm chart, operational scripts, and v1.28.0 release
notes. A filename review found no databases, generated backups, exports,
reports, environment files, private keys, or credentials.

The extracted archive was installed in a temporary directory and started with
an isolated SQLite database. Health, readiness, migrations, and version
reporting passed. The runtime reported version `1.28.0`.

The archive runtime created a native SQLite backup and the restore checker
confirmed its integrity and required tables.

## Published image

The published image digest is:

```text
sha256:9d9121a4208de9a37fc121101a3139526554ae87d53cccfe13d6d929e6df2b6f
```

The image was pulled by immutable digest and started with temporary SQLite and
backup volumes. Runtime version, migration state, fictional write behavior,
restart persistence, native backup, and restore validation passed.

The mutable `testing` tag resolves to the same digest. The `production` and
`latest` tags were not changed.

## PostgreSQL and transfer verification

The reviewed commit passed the PostgreSQL 16 and PostgreSQL 18 application
runtime suites. The parity suite covered portable transfers from SQLite to
PostgreSQL and from PostgreSQL to SQLite, including mandatory dry runs,
integrity checks, atomic imports, and rejection of changed packages.

The opt-in Compose test covered fresh PostgreSQL startup, fictional data,
restart persistence, unavailable-database behavior, invalid credentials, and
secret-safe logs. The default Compose and Helm configurations remained
SQLite-only. Helm PostgreSQL modes remained explicit opt-ins and were validated
through lint, render, install, upgrade, and rollback automation without a real
cluster change.

## Chart verification

The published chart was pulled anonymously from:

```text
oci://ghcr.io/hackepeter87/charts/betreuungskalender:0.9.0
```

The pulled chart reported:

```text
version: 0.9.0
appVersion: 1.28.0
```

Chart digest:

```text
sha256:abf0611bf3ba603baeb5cf419fa43249889e7a41f5c17c6990881188ddc7603e
```

## Result

The v1.28.0 archive, image, chart, versions, commit, checksums, and promoted
testing digest are consistent. SQLite compatibility, PostgreSQL parity,
portable transfer, failure handling, backup, restore, and restart behavior
passed the required automated and isolated published-artifact checks.
